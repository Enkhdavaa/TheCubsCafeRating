message RateRequest {
    string name = 1;
    string place = 2;
    string product = 3;
    string score = 4;
}

message RateResponse {
    string result = 1;
}

service UserService {
    rpc RateProduct(RateRequest) returns (RateResponse);
}